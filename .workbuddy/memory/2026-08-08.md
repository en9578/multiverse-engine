# 2026-08-08 工作日志

## 为初赛提交整理方案概述 + 技术方案说明

用户要去提交初赛，需要方案概述（一句话定义+核心功能+亮点）和技术方案说明。查阅 Obsidian 知识库 C:\dev\knowledge\project-document\multiverse-engine\：
- submission.md（7-27，方案概述完整）
- tech-solution.md（8-6 最新，65KB，技术方案详尽）

### 关键发现：架构升级为双服务分离
tech-solution.md 揭示项目已从纯 Java 单体升级为双服务：
- multiverse-api（Java 21/Spring Boot 3.4.4, 8080）：REST API、持久化、SSE、熔断
- multiverse-engine（Python 3.11/FastAPI+LangGraph, 8000）：多元宇宙生成、规则推演、R1推理、数据采集、LangGraph状态机
- 数据：MySQL（业务表）+ PostgreSQL（LangGraph checkpoint）+ Redis（幂等/进度/SSE总线）+ FAISS（向量）+ OSS
- 分离依据：LangChain/LangGraph Python 生态成熟度远超 Java；崩溃隔离；独立迭代

### 工程推进确认（vs 8-3 检索）
- 7 张表 DDL migration 已建（V1.0.0~V1.0.6，含 conversation 对话表 V1.0.6——之前建议的上下文隔离已落地）
- mapper 目录已建（mapper XML）
- multiverse-engine-python/ Python 工程已建（FastAPI+LangGraph）
- 百炼 4 模型 API 测试 4/4 通过

### 关键技术（最新）
- 状态机：LangGraph StateGraph（Python 主权）+ PostgresSaver checkpoint + Send API fan-out 5 宇宙 + interrupt 人工确认
- 可解释推演：RuleEngine + R1Enhancer（替代黑盒 ACE）
- 数据采集：Tavily/pytrends/frankfurter/预采集竞品JSON，封装 LangChain Tool
- 向量：FAISS + 百炼 text-embedding-v3（768维）
- 上下文隔离：LangGraph checkpoint + contextvars，task+universe+session 三级
- 服务间：HTTP REST + traceId 透传 + Resilience4j 熔断
- 可观测：LangSmith（LLM trace 评审可追溯）+ structlog
- 部署：Docker Compose 5 容器

### 产出
生成 `初赛提交稿.md`（C:\dev\project-package\minbao\），含：
- 方案概述：一句话定义 + 5 核心功能 + 4 亮点
- 技术方案说明：百炼 7 模型表 + 双服务架构 + 关键技术 + 完成阶段
基于最新 tech-solution.md 整合，可直接复制填初赛表单。
