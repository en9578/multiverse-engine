<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minbao.multiverse.dao.CompetitorReactionDAO">

    <insert id="insert" parameterType="com.minbao.multiverse.domain.entity.CompetitorReactionDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO competitor_reaction (universe_id, competitor_name, reaction_type, probability,
            impact, source, evidence, trace_id, gmt_create, gmt_modified)
        VALUES (#{universeId}, #{competitorName}, #{reactionType}, #{probability, jdbcType=DECIMAL},
            #{impact, jdbcType=VARCHAR}, #{source, jdbcType=VARCHAR}, #{evidence, jdbcType=VARCHAR},
            #{traceId}, NOW(), NOW())
    </insert>

    <select id="selectByUniverseId" parameterType="long"
            resultType="com.minbao.multiverse.domain.entity.CompetitorReactionDO">
        SELECT id, universe_id, competitor_name, reaction_type, probability, impact, source, evidence,
               trace_id, gmt_create, gmt_modified
        FROM competitor_reaction WHERE universe_id = #{universeId}
    </select>

</mapper>
