<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minbao.multiverse.dao.EntanglementEdgeDAO">

    <insert id="insert" parameterType="com.minbao.multiverse.domain.entity.EntanglementEdgeDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO entanglement_edge (source_universe_id, target_universe_id, weight, trace_id, gmt_create, gmt_modified)
        VALUES (#{sourceUniverseId}, #{targetUniverseId}, #{weight, jdbcType=DECIMAL}, #{traceId}, NOW(), NOW())
    </insert>

    <select id="selectByUniverseId" parameterType="long"
            resultType="com.minbao.multiverse.domain.entity.EntanglementEdgeDO">
        SELECT id, source_universe_id, target_universe_id, weight, trace_id, gmt_create, gmt_modified
        FROM entanglement_edge
        WHERE source_universe_id = #{universeId} OR target_universe_id = #{universeId}
    </select>

</mapper>