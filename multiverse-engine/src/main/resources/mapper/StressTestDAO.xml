<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minbao.multiverse.dao.StressTestDAO">

    <insert id="insert" parameterType="com.minbao.multiverse.domain.entity.StressTestDO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO stress_test (universe_id, storm, survival_rate, weakest_link, fix_suggestion,
            trace_id, gmt_create, gmt_modified)
        VALUES (#{universeId}, #{storm}, #{survivalRate, jdbcType=DECIMAL},
            #{weakestLink, jdbcType=VARCHAR}, #{fixSuggestion, jdbcType=VARCHAR},
            #{traceId}, NOW(), NOW())
    </insert>

    <select id="selectByUniverseId" parameterType="long"
            resultType="com.minbao.multiverse.domain.entity.StressTestDO">
        SELECT id, universe_id, storm, survival_rate, weakest_link, fix_suggestion,
               trace_id, gmt_create, gmt_modified
        FROM stress_test WHERE universe_id = #{universeId}
    </select>

</mapper>
